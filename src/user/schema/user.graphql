const { gql } = require('apollo-server')

const userSchema = gql `
  scalar DateTime

  type User {
    nome: String!
    ativo: Boolean!
    email: String
    role: Role!
    createAt: DateTime
    updateAt: DateTime
  }

  type Role {
    id: ID!
    type: String!
  }

  type Query {
    users: [User]
    user(id: ID!): User!
  }

  type Mutation {
    addUser (nome: String!, ativo: Boolean!, email: String, role: String!, createAt: DateTime, updateAt: DateTime): User!
    updateUser (id: ID!, nome: String!, ativo: Boolean!, email: String, role: String!, updateAt: DateTime): User!
    deleteUser (id: ID!): ID!
  }
`

module.exports = userSchema
